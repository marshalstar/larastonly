<?php namespace $NAMESPACE$;

use Illuminate\Routing\Controller;
use Redirect, Request;

class $NAME$ extends Controller {

	/**
	 * Lista $COLLECTION$
	 *
	 * @return Response
	 */
	public function index() {
		$$COLLECTION$ = $MODEL$::all();
		return view('$COLLECTION$.index', compact('$COLLECTION$'));
	}

	/**
	 * Mostra formulário de cadastro espefíco de $RESOURCE$
	 *
	 * @return Response
	 */
	public function create() {
		return view('$COLLECTION$.create');
	}

	/**
	 * Salva $RESOURCE$ espefíco no banco
	 *
	 * @return Response
	 */
	public function store() {
		$MODEL$::create(Request::get());
		return Redirect::route('$COLLECTION$.index');
	}

	/**
	 * Mostra $RESOURCE$ específico.
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function show($id) {
		$$RESOURCE$ = $MODEL$::findOrFail($id);
		return view('$COLLECTION$.show', compact('$RESOURCE$'));
	}

	/**
	 * Mostra formulário de edição espefícico de $RESOURCE$.
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function edit($id) {
		$$RESOURCE$ = $MODEL$::find($id);
		return view('$COLLECTION$.edit', compact('$RESOURCE$'));
	}

	/**
	 * Atualiza $RESOURCE$ específico no banco
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function update($id) {
		$$RESOURCE$ = $MODEL$::findOrFail($id);
		$$RESOURCE$->update(Request::get());
		return Redirect::route('$COLLECTION$.index');
	}

	/**
	 * Remove $RESOURCE$ específico do banco.
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function destroy($id) {
		$MODEL$::destroy($id);
		return Redirect::route('$COLLECTION$.index');
	}

}
